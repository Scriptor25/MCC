namespace example

define test_iterate() {
    constexpr array = [ 123, 456, "abc", "def", 789, { name: "max", age: 21 } ]
    foreach (const element : array) {
        !print("@s", `element: ${element}`)
    }
}

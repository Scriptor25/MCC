namespace example

define :print(targets, message) {
    ?`$tellraw $(targets) $(message)`
}

define :test_print(builtin) {
    return if (builtin) print(@s, "Hello World!")
    else $:print({ targets: "@s", message: "Hello World!" })
}

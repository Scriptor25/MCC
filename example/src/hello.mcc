namespace example

define :hello() {
    tellraw(@s, [ "Hello ", {
        selector: "@s",
        color: "gold"
    }, "!" ]);
}

define :hello_all() {
    tellraw(@s, `Hello ${@s}! Hello to all of ${@e[ type = !player ]}!`);
}
